#include "main.h"
/**
 * main - Entry point
 * function that adds two integers and returns the result
 * Return - Always 0 (success)
 * check code
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
