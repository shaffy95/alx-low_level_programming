#include <stdio.h>
/**
* main - determine if a number is positive, negative or zero.
* Return: Always 0 (success)
*/
int main(void)
{
	int n = 0;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
