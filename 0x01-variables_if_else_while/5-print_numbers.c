#include <stdio.h>
/**
* main - determine if a number is positive, negative or zero.
* Return: Always 0 (success)
*/
int main(void)
{
	int ch = 0;

	while (ch < 10)
	{
		putchar(ch);
		ch++;
	}

	putchar('\n');
	return (0);
}
